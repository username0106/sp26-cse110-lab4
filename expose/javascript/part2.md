1. line 12 would print 3 as i is a var so it can be accessed outside the for loop
2. line 13 would print 150 as the variable is var so it can be accessed outside the for loop
3. line 14 would also print 150 as the variable is also a var which can be accessed outside the for loop
4. the function would return [50, 100, 150] this would be the discounted prices from the original prices it works as the variable is inside the same function
5. there would be an error as i is not a let which is not accessable outside the for loop
6. there would be an error as the variable discountedPrice is also a let which is not accessable outside the for loop 
7. line 14 would print 150 as the let variable finalPrice is outside the for loop and inside the same function as the print statement
8. the function would return [50, 100, 150] this would be the discounted prices from the original prices it works even as let because it is already inside the same function inside the print statement
9. There would be an error as the variable i is a let not accessable outside the for loop
10. line 12 would print 3 as it is defined as prices.length which is 3 const functions similar access as let
11. the function would return [50, 100, 150] this would be the discounted prices from the original prices it works because const only blocks the variable to be assigned to a new array not what happens to the array itself
## Question 12

**A.** `student.name`

**B.** `student['Grad Year']`

**C.** `student.greeting()`

**D.** `student['Favorite Teacher'].name`

**E.** `student.courseLoad[0]`

## Question 13
**A.** `output would be '32' as string with + would be a string concatenation`
**B.** `output would be 1 as - has no string behavior so string 3 would be turned into 3 minus 2 which is 1`
**C.** `output would be 3 as null would be turned into 0 so output is 3`
**D.** `output would be '3null' as + with a string would be a string concatenation`
**E.** `output would be 4, true would be converted into 1`
**F.** `output would be 0 as both false and null is converted to 0`
**G.** `output would be '3undefined' the + would be a string concatenation`
**H.** `output would be NaN as 3 string would be turned into 3 but undefined would be converted to Nan`

## Question 14
**A.** `true as string 2 is converted to 2`
**B.** `false both are string so the compare is alphabetical`
**C.** `true as string 2 is converted to 2`
**D.** `false there is not type conversion with === and since they are different types it would be false`
**E.** `false true would be converted to 1 and compared to 2 would be false`
**F.** `true boolean(2) converts 2 to a boolean which anything non-zero would be a true and since both is true it would return true`

17. `The result is [2, 4, 6]. modifyArray is called with array = [1,2,3] and callback = doSomething. An empty array newArr = [] is created. The for loop iterates over each element and calls callback(array[i]) — which is just doSomething(array[i]) — and pushes the result into newArr. newArr is returned as [2, 4, 6].`
19. `Outputs would be 1 4 3 2 all in a new line`

 
